//! Workflow / saga orchestration.
//!
//! Reserved for telephony-specific saga flows (e.g. multi-step callback escalation). The skeleton
//! example saga was removed with the rest of the scaffold cruft; add a real workflow here when one
//! is needed, or delete this module + its declaration in `application/mod.rs` and the re-export in
//! `lib.rs`.
